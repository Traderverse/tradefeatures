% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trend.R
\name{add_ema}
\alias{add_ema}
\title{Add Exponential Moving Average}
\usage{
add_ema(data, n = 20, price = "close", name = NULL)
}
\arguments{
\item{data}{A data frame or market_tbl with price data}

\item{n}{Integer: number of periods for EMA (default: 20)}

\item{price}{Character: column name to use for calculation (default: "close")}

\item{name}{Character: name for new column (default: "ema_{n}")}
}
\value{
Data with new EMA column added
}
\description{
Add exponential moving average as a new column to market data.
}
\examples{
\dontrun{
library(tradeio)

# Add 12-day EMA
prices <- fetch_prices("AAPL", from = "2024-01-01") |>
  add_ema(12)

# Add multiple EMAs
prices <- fetch_prices("AAPL", from = "2024-01-01") |>
  add_ema(12, name = "ema_fast") |>
  add_ema(26, name = "ema_slow")
}
}
